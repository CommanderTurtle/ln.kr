# Vendored browser code

`jsfuck.js` is JSFuck 0.5.0 by Martin Kleppe, vendored from
<https://github.com/aemkei/jsfuck>. The upstream project is distributed under
the WTFPL. It is loaded locally so an ln.kr viewer never needs a CDN.
